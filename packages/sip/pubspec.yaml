name: sip
description: A command-line tool to manage Dart mono-repos.
version: 0.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.2
  file: ^7.0.0
  get_it: ^7.6.6
  path: ^1.9.0
  sip_script_runner:
  yaml: ^3.1.2

dev_dependencies:
  build_runner: ^2.4.8
  build_version: ^2.1.1
  test: ^1.25.1

executables:
  sip:
